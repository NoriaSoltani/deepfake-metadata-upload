# Deepfake Metadata Uploader

A simple script for loading merged JSON metadata into a MongoDB collection.
Useful when working with custom deepfake datasets stored locally.

## How to use
1. Update the JSON file path at the top of the script.
2. Make sure MongoDB is running.
3. Install dependencies:

   pip install -r requirements.txt

4. Run the script:

   python upload_metadata.py
